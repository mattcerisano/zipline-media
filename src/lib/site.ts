/**
 * The site's canonical origin, used for metadataBase, Open Graph URLs, the
 * sitemap and robots.txt. www.zipline.media 308-redirects here (set in Vercel
 * → Domains), so this must stay the apex.
 */
export const SITE_URL = 'https://zipline.media';
